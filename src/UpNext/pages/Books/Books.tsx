import Queue from "../../components/QueuePages/Queue/Queue";

/**
 * Displays the queue of books and allows users to manage their book queue.
 */
export default function Books() {
  return <Queue mediaType="Book" />;
}
