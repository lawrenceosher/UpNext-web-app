import Queue from "../../components/QueuePages/Queue/Queue";

/**
 * Displays the queue of games and allows users to manage their game queue.
 */
export default function Games() {
  return <Queue mediaType="VideoGame" />;
}
