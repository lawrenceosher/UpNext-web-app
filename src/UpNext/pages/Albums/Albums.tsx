import Queue from "../../components/QueuePages/Queue/Queue";

/**
 * Displays the queue of albums and allows users to manage their album queue.
 */
export default function Albums() {
  return <Queue mediaType="Album" />;
}
