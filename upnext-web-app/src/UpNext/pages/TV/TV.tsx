import Queue from "../../components/QueuePages/Queue/Queue";

/**
 * Displays the queue of TV Shows and allows users to manage their queue.
 */
export default function TV() {
  return <Queue mediaType="TV" />;
}
